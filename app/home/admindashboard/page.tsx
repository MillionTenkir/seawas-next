import React from "react";

const page = () => {
  return (
    <div>
      Inner dashboard Inner dashboard Inner dashboard Inner dashboard Inner
      dashboard
    </div>
  );
};

export default page;
